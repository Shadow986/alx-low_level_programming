#include "main.h"

/**
 * mul - function that multiplies two integers
 * @x: x - Var
 * @y: y - Var
 * Return: the sum of the multiplication
 */

int mul(int x, int y)
{
	int mul;

	mul = x * b;
	return (mul);
}
