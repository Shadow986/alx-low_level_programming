#include "main.h"


/**
 * _islower - a function that checks
 * for lowercase character
 * @c: the character to check
 * Return: 1 if c is lowercase, and if
 * its not the return 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
