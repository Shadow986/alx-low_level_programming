#include <stdio.h>

/**
 * main - Entry point for program that
 * prints the alphabates in lowercase
 * Return: Everythime 0 (success)
 */

int main(void)
{
	char x;

	x = 'b';
	while
		(x <= 'z') {
			putchar(x);
			x++;
		}
	putchar('\n';
	return (0);
}
